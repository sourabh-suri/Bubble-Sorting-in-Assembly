	MOV R0, #09H
OUTLOOP	MOV DPTR, #9000H
	MOV R1, #09H
INLOOP	MOVX A, @DPTR
	MOV B, A
	INC DPTR
	MOVX A, @DPTR
	CLR C
	MOV R2, A
	SUBB A, B
	JNC SKIP
	MOV A, B
	MOVX @DPTR, A
	DEC DPL
	MOV A, R2
	MOVX @DPTR, A
SKIP	DJNZ R1, INLOOP
	DJNZ R0, OUTLOOP
	END
